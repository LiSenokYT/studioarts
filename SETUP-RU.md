# Инструкция по настройке проекта

## Шаг 1: Настройка Supabase

### 1.1 Создание проекта

1. Перейдите на https://supabase.com
2. Нажмите "Start your project"
3. Войдите через GitHub
4. Нажмите "New project"
5. Заполните:
   - Name: `artstudio` (или любое другое имя)
   - Database Password: придумайте надежный пароль
   - Region: выберите ближайший регион
6. Нажмите "Create new project"
7. Дождитесь создания проекта (1-2 минуты)

### 1.2 Получение ключей

1. В боковом меню нажмите на иконку шестеренки (Settings)
2. Выберите "API"
3. Скопируйте:
   - **Project URL** (например: https://xxxxx.supabase.co)
   - **anon public** ключ (длинная строка)

### 1.3 Создание базы данных

1. В боковом меню выберите "SQL Editor"
2. Нажмите "New query"
3. Откройте файл `supabase-schema.sql` из проекта
4. Скопируйте весь код и вставьте в редактор
5. Нажмите "Run" (или Ctrl+Enter)
6. Дождитесь выполнения (должно появиться "Success")

### 1.4 Настройка Storage (хранилище файлов)

1. В боковом меню выберите "Storage"
2. Нажмите "Create a new bucket"
3. Создайте три bucket:

**Bucket 1: order-images**
- Name: `order-images`
- Public bucket: ✅ (включить)
- Нажмите "Create bucket"

**Bucket 2: final-works**
- Name: `final-works`
- Public bucket: ✅ (включить)
- Нажмите "Create bucket"

**Bucket 3: gallery-images**
- Name: `gallery-images`
- Public bucket: ✅ (включить)
- Нажмите "Create bucket"

### 1.5 Настройка политик Storage

Для каждого bucket нужно настроить политики доступа:

1. Нажмите на bucket
2. Перейдите на вкладку "Policies"
3. Нажмите "New policy"
4. Выберите "For full customization"
5. Создайте политику:

**Для чтения (SELECT):**
- Policy name: `Public read access`
- Target roles: `public`
- Policy definition: `true`
- Нажмите "Review" → "Save policy"

**Для записи (INSERT):**
- Policy name: `Authenticated users can upload`
- Target roles: `authenticated`
- Policy definition: `true`
- Нажмите "Review" → "Save policy"

Повторите для всех трех buckets.

## Шаг 2: Настройка проекта

### 2.1 Установка зависимостей

Откройте терминал в папке проекта и выполните:

\`\`\`bash
npm install
\`\`\`

### 2.2 Настройка переменных окружения

1. Откройте файл `.env.local` в корне проекта
2. Замените значения на ваши:

\`\`\`env
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_ключ_здесь
\`\`\`

### 2.3 Запуск проекта

\`\`\`bash
npm run dev
\`\`\`

Откройте браузер и перейдите на http://localhost:3000

## Шаг 3: Создание первого пользователя

1. Перейдите на http://localhost:3000/auth/register
2. Зарегистрируйтесь:
   - Полное имя: Ваше имя
   - Email: ваш email
   - Пароль: минимум 6 символов
3. Нажмите "Зарегистрироваться"

## Шаг 4: Назначение роли художницы

### 4.1 Через Supabase Dashboard

1. Перейдите в Supabase Dashboard
2. Выберите "Table Editor" в боковом меню
3. Выберите таблицу "profiles"
4. Найдите вашего пользователя
5. Нажмите на строку для редактирования
6. Измените поле `role` с `user` на `artist`
7. Нажмите "Save"

### 4.2 Через SQL

1. Перейдите в "SQL Editor"
2. Выполните запрос:

\`\`\`sql
UPDATE profiles 
SET role = 'artist' 
WHERE email = 'ваш_email@example.com';
\`\`\`

## Шаг 5: Создание администратора

Аналогично шагу 4, но измените роль на `admin`:

\`\`\`sql
UPDATE profiles 
SET role = 'admin' 
WHERE email = 'admin_email@example.com';
\`\`\`

## Шаг 6: Тестирование функционала

### Как пользователь:
1. Войдите в систему
2. Перейдите в "Личный кабинет"
3. Нажмите "Новый заказ"
4. Заполните форму и отправьте

### Как художница:
1. Войдите под аккаунтом с ролью artist
2. В личном кабинете увидите все заказы
3. Можете принять/отклонить заказ
4. После принятия откроется чат
5. Можете выставить цену
6. После подтверждения оплаты - завершить заказ

### Как администратор:
1. Войдите под аккаунтом с ролью admin
2. Увидите панель администратора
3. Доступ ко всем функциям системы

## Шаг 7: Деплой на Vercel

### 7.1 Подготовка

1. Создайте репозиторий на GitHub
2. Загрузите код:

\`\`\`bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
\`\`\`

### 7.2 Деплой

1. Перейдите на https://vercel.com
2. Войдите через GitHub
3. Нажмите "Add New" → "Project"
4. Выберите ваш репозиторий
5. В разделе "Environment Variables" добавьте:
   - `NEXT_PUBLIC_SUPABASE_URL` = ваш URL
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = ваш ключ
6. Нажмите "Deploy"
7. Дождитесь завершения (2-3 минуты)

### 7.3 Настройка домена (опционально)

1. В настройках проекта на Vercel
2. Перейдите в "Domains"
3. Добавьте свой домен
4. Следуйте инструкциям для настройки DNS

## Возможные проблемы и решения

### Ошибка "Invalid API key"
- Проверьте правильность ключей в `.env.local`
- Убедитесь, что файл `.env.local` находится в корне проекта
- Перезапустите сервер разработки

### Ошибка при создании заказа
- Проверьте, что все buckets созданы
- Проверьте политики доступа к Storage
- Убедитесь, что пользователь авторизован

### Чат не работает
- Проверьте, что Realtime включен в Supabase
- Убедитесь, что заказ в статусе "discussing", "payment_pending" или "in_progress"

### Не приходят уведомления
- Функция уведомлений пока не реализована
- Можно добавить через Supabase Edge Functions

## Дополнительные настройки

### Изменение цветов

Откройте файлы компонентов и измените цвета:
- `#A682E6` - сиреневый
- `#BDBFF2` - лавандовый
- `#FFD9E6` - розовый

### Добавление логотипа

1. Поместите файл логотипа в папку `public`
2. Откройте `components/navigation/navbar.tsx`
3. Замените текст "ArtStudio" на компонент Image

### Настройка email уведомлений

1. В Supabase перейдите в "Authentication" → "Email Templates"
2. Настройте шаблоны писем
3. Добавьте SMTP настройки в "Settings" → "Auth"

## Контакты для поддержки

Если возникли вопросы:
1. Проверьте документацию Supabase: https://supabase.com/docs
2. Проверьте документацию Next.js: https://nextjs.org/docs
3. Создайте issue в репозитории проекта

Удачи с проектом! 🎨✨
