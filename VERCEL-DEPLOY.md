# Инструкция по деплою на Vercel

## ✅ Проект готов к деплою!

Сборка прошла успешно без ошибок. Все необходимые файлы на месте.

## Шаги для деплоя на Vercel:

### 1. Подготовка (уже выполнено)
- ✅ Проект собирается без ошибок (`npm run build`)
- ✅ `.gitignore` настроен правильно (`.env*` файлы не будут загружены)
- ✅ Все зависимости установлены
- ✅ TypeScript конфигурация корректна

### 2. Загрузка на Vercel

#### Вариант A: Через GitHub (рекомендуется)
1. Создайте репозиторий на GitHub
2. Загрузите код:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin <your-repo-url>
   git push -u origin main
   ```
3. Зайдите на [vercel.com](https://vercel.com)
4. Нажмите "Add New Project"
5. Импортируйте ваш GitHub репозиторий
6. Vercel автоматически определит Next.js проект

#### Вариант B: Через Vercel CLI
1. Установите Vercel CLI:
   ```bash
   npm i -g vercel
   ```
2. Войдите в аккаунт:
   ```bash
   vercel login
   ```
3. Деплой:
   ```bash
   vercel
   ```

### 3. Настройка переменных окружения в Vercel

**ВАЖНО!** После создания проекта на Vercel, добавьте переменные окружения:

1. Перейдите в Settings → Environment Variables
2. Добавьте следующие переменные:

```
NEXT_PUBLIC_SUPABASE_URL=https://ejynnqrzbhzejiecfvjj.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVqeW5ucXJ6Ymh6ZWppZWNmdmpqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA4MzcxODEsImV4cCI6MjA4NjQxMzE4MX0.IWXFHdstqZP2F_fpOiYb9Boxpy7KVR4wmk-Skab8sqY
```

3. Выберите для каких окружений применить (Production, Preview, Development)
4. Сохраните

### 4. После деплоя

1. **Обновите URL в Supabase:**
   - Зайдите в Supabase Dashboard
   - Authentication → URL Configuration
   - Добавьте ваш Vercel URL в "Site URL" и "Redirect URLs"
   - Формат: `https://your-project.vercel.app`

2. **Проверьте работу:**
   - Регистрация/вход
   - Создание заказов
   - Загрузка изображений
   - Чат
   - Галерея

### 5. Возможные проблемы и решения

#### Проблема: "Module not found"
**Решение:** Убедитесь, что все зависимости в `package.json` и выполните `npm install`

#### Проблема: Ошибки с изображениями
**Решение:** Проверьте, что домен Supabase добавлен в `next.config.ts` (уже добавлен)

#### Проблема: Middleware warning
**Решение:** Это предупреждение можно игнорировать, оно не влияет на работу

#### Проблема: Ошибки аутентификации
**Решение:** 
1. Проверьте переменные окружения в Vercel
2. Добавьте Vercel URL в Supabase Redirect URLs

### 6. Автоматические деплои

После первого деплоя через GitHub:
- Каждый push в `main` ветку = автоматический деплой в Production
- Каждый push в другие ветки = Preview деплой
- Pull Request = Preview деплой с уникальным URL

## Дополнительные настройки (опционально)

### Кастомный домен
1. В Vercel: Settings → Domains
2. Добавьте ваш домен
3. Настройте DNS записи согласно инструкциям Vercel
4. Обновите URL в Supabase

### Оптимизация
- Vercel автоматически оптимизирует изображения
- Edge Functions работают из коробки
- CDN настроен автоматически

## Готово! 🎉

Ваш проект готов к деплою. Следуйте инструкциям выше и всё будет работать.

Если возникнут проблемы - проверьте логи в Vercel Dashboard.
