# Часто задаваемые вопросы (FAQ)

## 🚀 Начало работы

### Как запустить проект локально?

1. Установите зависимости:
\`\`\`bash
npm install
\`\`\`

2. Настройте `.env.local` с вашими ключами Supabase

3. Запустите:
\`\`\`bash
npm run dev
\`\`\`

### Где взять ключи Supabase?

1. Перейдите на https://supabase.com
2. Создайте проект
3. Settings → API → скопируйте URL и anon key

## 👥 Пользователи и роли

### Как создать художницу?

1. Зарегистрируйте обычного пользователя
2. В Supabase Dashboard → Table Editor → profiles
3. Найдите пользователя и измените `role` на `artist`

Или через SQL:
\`\`\`sql
UPDATE profiles SET role = 'artist' WHERE email = 'artist@example.com';
\`\`\`

### Как создать администратора?

Аналогично художнице, но роль `admin`:
\`\`\`sql
UPDATE profiles SET role = 'admin' WHERE email = 'admin@example.com';
\`\`\`

### Можно ли иметь несколько художниц?

Да! Просто назначьте роль `artist` нескольким пользователям. Все они смогут:
- Принимать заказы
- Управлять галереей
- Общаться с клиентами

### Как заблокировать пользователя?

Через админ панель или SQL:
\`\`\`sql
UPDATE profiles SET is_banned = true WHERE id = 'user_id';
\`\`\`

## 📦 Заказы

### Почему не могу создать заказ?

Проверьте:
1. Вы авторизованы?
2. Storage buckets созданы?
3. Политики доступа настроены?

### Как отменить заказ?

Сейчас отмена доступна только через базу данных:
\`\`\`sql
UPDATE orders SET status = 'cancelled' WHERE id = 'order_id';
\`\`\`

Можно добавить кнопку отмены в интерфейс.

### Почему чат не работает?

Чат доступен только когда заказ в статусе:
- `discussing`
- `payment_pending`
- `in_progress`

Художница должна сначала принять заказ.

### Как загрузить готовую работу?

1. Откройте заказ со статусом `in_progress`
2. Нажмите "Завершить заказ"
3. Загрузите файл
4. Подтвердите

Работа будет доступна для скачивания клиенту.

## 🎨 Галерея

### Как добавить работу в галерею?

Пока через код или базу данных. Можно создать страницу управления галереей:

\`\`\`typescript
// Пример добавления в галерею
const { data, error } = await supabase
  .from('gallery_items')
  .insert({
    title: 'Название работы',
    description: 'Описание',
    image_url: 'url_изображения',
    created_by: user.id
  });
\`\`\`

### Как удалить работу из галереи?

Через базу данных или добавьте функционал в интерфейс:

\`\`\`typescript
await supabase
  .from('gallery_items')
  .delete()
  .eq('id', 'item_id');
\`\`\`

## 💾 База данных

### Как сбросить базу данных?

1. В Supabase Dashboard → SQL Editor
2. Выполните:
\`\`\`sql
-- Удалить все данные
TRUNCATE profiles, gallery_items, orders, messages, testimonials CASCADE;
\`\`\`

⚠️ Это удалит ВСЕ данные!

### Как сделать бэкап?

1. В Supabase Dashboard → Database → Backups
2. Нажмите "Create backup"
3. Или экспортируйте через SQL:
\`\`\`bash
pg_dump -h db.xxxxx.supabase.co -U postgres -d postgres > backup.sql
\`\`\`

### Как восстановить из бэкапа?

1. В Supabase Dashboard → Database → Backups
2. Выберите бэкап → "Restore"

## 🎨 Дизайн и стили

### Как изменить цвета?

Найдите и замените в компонентах:
- `#A682E6` → ваш цвет 1
- `#BDBFF2` → ваш цвет 2
- `#FFD9E6` → ваш цвет 3

Или создайте CSS переменные в `globals.css`:
\`\`\`css
:root {
  --color-primary: #A682E6;
  --color-secondary: #BDBFF2;
  --color-accent: #FFD9E6;
}
\`\`\`

### Как добавить логотип?

1. Поместите файл в `/public/logo.png`
2. В `components/navigation/navbar.tsx`:

\`\`\`tsx
import Image from 'next/image';

// Замените текст на:
<Image src="/logo.png" alt="Logo" width={120} height={40} />
\`\`\`

### Как изменить шрифт?

В `app/layout.tsx` измените импорт шрифта:

\`\`\`tsx
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin', 'cyrillic'] });
\`\`\`

## 🔧 Технические вопросы

### Ошибка "Invalid API key"

1. Проверьте `.env.local`
2. Убедитесь, что ключи правильные
3. Перезапустите сервер: `npm run dev`

### Ошибка при загрузке файлов

1. Проверьте, что buckets созданы
2. Проверьте политики Storage
3. Проверьте размер файла (лимит 50MB по умолчанию)

### Как увеличить лимит размера файла?

В Supabase Dashboard → Storage → Settings:
- Измените `File size limit`

### Realtime не работает

1. Проверьте, что Realtime включен в Supabase
2. В Database → Replication → включите таблицы:
   - `messages`
   - `orders`

### Как добавить email уведомления?

Используйте Supabase Edge Functions:

1. Создайте функцию:
\`\`\`typescript
// supabase/functions/send-email/index.ts
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts';

serve(async (req) => {
  // Логика отправки email
});
\`\`\`

2. Деплой:
\`\`\`bash
supabase functions deploy send-email
\`\`\`

## 🚀 Деплой

### Как задеплоить на Vercel?

1. Загрузите код на GitHub
2. Vercel → Import Project
3. Добавьте переменные окружения
4. Deploy

### Как настроить свой домен?

1. В Vercel → Settings → Domains
2. Добавьте домен
3. Настройте DNS записи у регистратора

### Ошибки после деплоя

1. Проверьте переменные окружения
2. Проверьте логи: Vercel → Deployments → Logs
3. Убедитесь, что Supabase доступен

## 💰 Оплата

### Как добавить систему оплаты?

Рекомендуем Stripe:

1. Установите:
\`\`\`bash
npm install @stripe/stripe-js stripe
\`\`\`

2. Создайте API route:
\`\`\`typescript
// app/api/create-payment/route.ts
import Stripe from 'stripe';

export async function POST(req: Request) {
  const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!);
  // Создание платежа
}
\`\`\`

3. Интегрируйте в компонент заказа

### Какие способы оплаты поддерживаются?

Зависит от платежной системы:
- **Stripe**: карты, Apple Pay, Google Pay
- **PayPal**: PayPal аккаунт, карты
- **ЮKassa**: карты, СБП, электронные кошельки

## 📊 Аналитика

### Как добавить Google Analytics?

1. Установите:
\`\`\`bash
npm install @next/third-parties
\`\`\`

2. В `app/layout.tsx`:
\`\`\`tsx
import { GoogleAnalytics } from '@next/third-parties/google';

export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        {children}
        <GoogleAnalytics gaId="G-XXXXXXXXXX" />
      </body>
    </html>
  );
}
\`\`\`

### Как отслеживать заказы?

Создайте страницу статистики:

\`\`\`typescript
const { data: stats } = await supabase
  .from('orders')
  .select('status, count(*)')
  .group('status');
\`\`\`

## 🔒 Безопасность

### Как защитить админ панель?

RLS политики уже настроены. Дополнительно можно:

1. Добавить middleware:
\`\`\`typescript
// middleware.ts
export function middleware(request: NextRequest) {
  // Проверка роли
}
\`\`\`

2. Использовать IP whitelist в Vercel

### Как защититься от спама?

1. Добавьте rate limiting
2. Используйте reCAPTCHA
3. Модерация заказов

## 📱 Мобильное приложение

### Можно ли сделать мобильное приложение?

Да! Варианты:
1. **PWA** - добавьте manifest.json
2. **React Native** - переиспользуйте логику
3. **Capacitor** - оберните веб-версию

## 🆘 Поддержка

### Где получить помощь?

1. Документация Supabase: https://supabase.com/docs
2. Документация Next.js: https://nextjs.org/docs
3. GitHub Issues проекта
4. Stack Overflow

### Как сообщить об ошибке?

Создайте issue на GitHub с:
- Описанием проблемы
- Шагами воспроизведения
- Скриншотами
- Версией браузера/ОС

## 🎓 Обучение

### Где изучить Next.js?

- Официальная документация: https://nextjs.org/learn
- YouTube каналы: Fireship, Web Dev Simplified
- Курсы: Udemy, Frontend Masters

### Где изучить Supabase?

- Официальная документация: https://supabase.com/docs
- YouTube: Supabase официальный канал
- Примеры: https://github.com/supabase/supabase/tree/master/examples

## 🔄 Обновления

### Как обновить зависимости?

\`\`\`bash
npm update
\`\`\`

Или для мажорных версий:
\`\`\`bash
npm install next@latest react@latest react-dom@latest
\`\`\`

### Как мигрировать базу данных?

1. Создайте миграцию:
\`\`\`sql
-- supabase/migrations/20240101_add_column.sql
ALTER TABLE orders ADD COLUMN new_field TEXT;
\`\`\`

2. Примените:
\`\`\`bash
supabase db push
\`\`\`

---

Не нашли ответ? Создайте issue на GitHub! 🚀
