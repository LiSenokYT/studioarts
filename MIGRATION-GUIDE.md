# 🔄 Руководство по миграции

## Если вы уже создали базу данных

Если вы уже выполнили `supabase-schema.sql` и база данных создана, вам нужно добавить новые поля для системы оплаты.

### Шаг 1: Выполните миграцию

1. Откройте Supabase Dashboard
2. Перейдите в SQL Editor
3. Скопируйте содержимое файла `supabase-migration-payment.sql`
4. Вставьте и выполните (Run)

### Шаг 2: Проверьте изменения

Выполните запрос для проверки:

\`\`\`sql
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'orders' 
AND column_name IN ('payment_screenshot_url', 'payment_rejection_reason');
\`\`\`

Должны увидеть два новых поля:
- `payment_screenshot_url` (text)
- `payment_rejection_reason` (text)

### Шаг 3: Обновите TypeScript типы

Файл `lib/supabase/database.types.ts` уже обновлен. Если у вас были изменения, убедитесь что добавлены поля:

\`\`\`typescript
payment_screenshot_url: string | null
payment_rejection_reason: string | null
\`\`\`

## Если вы еще не создали базу данных

Просто используйте обновленный `supabase-schema.sql` - все поля уже включены!

## Что изменилось в системе оплаты

### Было:
1. Художница выставляет цену
2. Пользователь нажимает "Подтвердить оплату"
3. Статус меняется на "В работе"

### Стало:
1. Художница выставляет цену
2. Пользователь видит реквизиты для оплаты
3. Пользователь делает перевод и загружает скриншот
4. Художница проверяет скриншот
5. Художница подтверждает или отклоняет оплату
6. При подтверждении → "В работе"
7. При отклонении → "Отменен" (чат закрывается)

## Новые статусы

Статус `cancelled` теперь используется для отмененных заказов (когда оплата отклонена).

## Обновление компонентов

Все компоненты уже обновлены:
- ✅ `app/dashboard/orders/[id]/page.tsx` - страница заказа
- ✅ `components/payment/payment-info.tsx` - компонент оплаты (НОВЫЙ)
- ✅ `lib/supabase/database.types.ts` - типы

## Настройка реквизитов

⚠️ **ВАЖНО:** Не забудьте настроить реквизиты оплаты!

См. [PAYMENT-SETUP.md](PAYMENT-SETUP.md)

## Проверка работы

### 1. Создайте тестовый заказ
\`\`\`bash
# Как пользователь
- Создайте заказ
- Дождитесь принятия
\`\`\`

### 2. Выставьте цену
\`\`\`bash
# Как художница
- Откройте заказ
- Выставьте цену (например, 100 ₽)
\`\`\`

### 3. Проверьте реквизиты
\`\`\`bash
# Как пользователь
- Обновите страницу
- Должны увидеть реквизиты для оплаты
- Проверьте все способы оплаты
\`\`\`

### 4. Загрузите скриншот
\`\`\`bash
# Как пользователь
- Загрузите любое изображение
- Проверьте, что оно отображается
\`\`\`

### 5. Подтвердите оплату
\`\`\`bash
# Как художница
- Обновите страницу заказа
- Увидите скриншот от пользователя
- Нажмите "Подтвердить оплату"
- Статус должен измениться на "В работе"
\`\`\`

### 6. Протестируйте отклонение
\`\`\`bash
# Создайте новый заказ
# Повторите шаги 1-4
# Как художница нажмите "Отклонить оплату"
# Укажите причину
# Статус должен измениться на "Отменен"
# Чат должен стать недоступен
\`\`\`

## Откат изменений (если нужно)

Если что-то пошло не так, можно откатить изменения:

\`\`\`sql
-- Удалить новые поля
ALTER TABLE orders 
DROP COLUMN IF EXISTS payment_screenshot_url,
DROP COLUMN IF EXISTS payment_rejection_reason;
\`\`\`

⚠️ **Внимание:** Это удалит все загруженные скриншоты оплаты!

## Поддержка

Если возникли проблемы:
1. Проверьте [FAQ.md](FAQ.md)
2. Проверьте [PAYMENT-SETUP.md](PAYMENT-SETUP.md)
3. Проверьте логи в Supabase Dashboard
4. Создайте issue на GitHub

---

Миграция завершена! Система оплаты готова к использованию! 💳✨
