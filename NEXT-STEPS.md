# 🎯 Следующие шаги после установки

## ✅ Проект установлен! Что дальше?

### 1. Проверьте базовый функционал (5 минут)

#### Тест регистрации:
\`\`\`bash
# Откройте http://localhost:3000/auth/register
# Зарегистрируйте пользователя
# Проверьте, что попали в личный кабинет
\`\`\`

#### Тест создания заказа:
\`\`\`bash
# В личном кабинете нажмите "Новый заказ"
# Заполните форму
# Загрузите изображение (опционально)
# Отправьте заказ
\`\`\`

### 2. Настройте роли (2 минуты)

#### Создайте художницу:
\`\`\`sql
-- В Supabase SQL Editor
UPDATE profiles 
SET role = 'artist' 
WHERE email = 'ваш_email@example.com';
\`\`\`

#### Создайте администратора:
\`\`\`sql
UPDATE profiles 
SET role = 'admin' 
WHERE email = 'admin@example.com';
\`\`\`

### 3. Протестируйте полный цикл заказа (10 минут)

1. **Как пользователь:**
   - Создайте заказ
   - Дождитесь принятия

2. **Как художница:**
   - Войдите под аккаунтом художницы
   - Примите заказ
   - Напишите в чат
   - Выставите цену

3. **Как пользователь:**
   - Подтвердите оплату
   - Общайтесь в чате

4. **Как художница:**
   - Загрузите готовую работу
   - Завершите заказ

5. **Как пользователь:**
   - Скачайте готовую работу

### 4. Добавьте контент (15 минут)

#### Добавьте работы в галерею:
\`\`\`sql
INSERT INTO gallery_items (
  title,
  description,
  image_url,
  created_by
) VALUES (
  'Портрет в стиле аниме',
  'Цифровой портрет персонажа',
  'https://example.com/image.jpg',
  (SELECT id FROM profiles WHERE role = 'artist' LIMIT 1)
);
\`\`\`

#### Добавьте тестовые отзывы:
\`\`\`sql
INSERT INTO testimonials (
  user_id,
  content,
  rating,
  is_approved
) VALUES (
  (SELECT id FROM profiles WHERE role = 'user' LIMIT 1),
  'Отличная работа! Очень довольна результатом!',
  5,
  true
);
\`\`\`

### 5. Настройте дизайн под себя (30 минут)

#### Измените цвета:
Найдите и замените в компонентах:
- `#A682E6` 